[project]
name = "agent-smith"
version = "0.1.0"
description = "Agentic framework solving MBPP & SWE-bench tasks (42 School project)"
requires-python = ">=3.10,<3.11"
dependencies = [
    "pydantic>=2.0",
    "requests>=2.31",
    "mcp>=1.0,<2.0",          # Model Context Protocol SDK — pip install mcp
    # TODO: add your MCP transport deps if needed (e.g. "mcp[cli]")
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "ruff>=0.5",
]

# uv 会用这个把 `uv run sandbox` 变成一条可执行命令，
# 指向 sandbox/cli.py 里的 main() 函数
[project.scripts]
sandbox = "sandbox.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["common", "sandbox", "agent_mbpp", "agent_swebench"]
